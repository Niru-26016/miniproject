# instructions 

1. add OPENAI_KEY in .env file
2. create virtual environment 
- python venv .venv - to create (cmd)
- .venv\scripts\activate - to activate(cmd)
3. run : pip install -r requirements.txt
4. run : uvicorn main:app --reload
5. visit http://127.0.0.1:8000/docs to test endpoints

